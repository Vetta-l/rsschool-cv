$(document).ready(function() {
    var modal = {
        self: $('#modal'),
    
        showModal: function(content) {
            this.self.find('#dialog').html(content);
            this.self.fadeIn(200);
        }
    }
});